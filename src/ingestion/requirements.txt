# Local dev — chargement .env (sans effet si vars déjà injectées par Cloud Run)
python-dotenv==1.0.1

# GCP clients
google-cloud-storage==2.18.2
google-cloud-secret-manager==2.20.2

# HTTP / APIs
requests==2.32.3

# Data / Parquet
pandas==2.2.3
pyarrow==17.0.0